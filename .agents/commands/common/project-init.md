这是项目规范初始化入口。目标不是只生成 `01/03`，而是按当前项目事实补齐本次应写的全部规范文件。

先读取并严格遵守：

- `.agents/skills/common/project-init/SKILL.md`

执行要求：

1. 先检查 `.agents/rules/` 下哪些规则文件缺失。
2. 固定写入：
   - `.agents/rules/01-项目概述.md`
   - `.agents/rules/03-项目结构.md`
   - `context/PROJECT.md`
3. 若项目存在 `openspec/`，同步 `openspec/project.md`。
4. 若 `.agents/rules/` 下缺失以下任意文件，必须在本次一并补生成：
   - `04-组件规范.md`
   - `05-API规范.md`
   - `06-路由规范.md`
   - `07-状态管理.md`
   - `09-样式规范.md`

绝对红线：

- 不能只写 `01/03 + PROJECT` 就结束
- 不能把缺失的 `04/05/06/07/09` 只写进摘要或建议，不落盘
- 对已存在的 `04/05/06/07/09` 不覆盖
- 内容必须基于当前仓库实际代码、目录和依赖，不能照搬通用模板

把用户触发本命令视为明确授权：信息足够时直接完成写入；只有在项目事实明显不足、无法判断时，才先给出简短确认摘要。
