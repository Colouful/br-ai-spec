# 角色目录说明

本目录用于定义“专家角色”的源文件。

当前阶段先保持最小 MVP，只放 3 个角色：

- `requirement-analyst.md`
- `frontend-implementer.md`
- `code-guardian.md`

原则：

- 角色文件描述职责与交接，不直接替代 `skills`
- 角色负责“谁来做、做什么、交给谁”
- `skills` 负责“具体怎么做”

后续如需扩展，再按相同结构新增角色文件即可，不需要推翻目录。
