---
alwaysApply: false
description: 项目的状态管理规范，使用 Pinia 进行全局状态管理。当需要新增或重构状态管理时读取此规则。
---

# 状态管理规范（NON-NEGOTIABLE）

## 基础规范

- 状态管理必须使用 **Pinia**
- 所有 store 文件必须放在 `src/stores/` 目录下
- **禁止在 `src` 下新建其他目录存放 store**
- 禁止使用 Vuex 或其他状态管理方案

## Store 编写规范

- 统一使用 **Setup Store**（组合式语法），禁止 Options Store
- 每个 store 暴露 `useXxxStore` 命名的函数
- Store 中不要直接耦合具体 UI 组件，只存纯数据与业务行为

```typescript
// src/stores/user.ts
export const useUserStore = defineStore('user', () => {
  const userInfo = ref<UserInfo | null>(null)
  const isLoggedIn = computed(() => !!userInfo.value)

  async function fetchUser() {
    userInfo.value = await getUserInfo()
  }

  function resetUser() {
    userInfo.value = null
  }

  return { userInfo, isLoggedIn, fetchUser, resetUser }
})
```

## 目录结构

```
src/stores/
├── user.ts        # useUserStore
├── app.ts         # useAppStore（主题、语言等全局配置）
├── permission.ts  # usePermissionStore
└── index.ts       # 统一导出（可选）
```

## 在组件中使用

解构响应式状态时必须使用 `storeToRefs`，保持响应性；Action 方法直接从 store 实例解构：

```typescript
const userStore = useUserStore()
const { userInfo, isLoggedIn } = storeToRefs(userStore)
const { fetchUser } = userStore
```

## 持久化

当状态需要跨页面刷新保留时，使用 `pinia-plugin-persistedstate`，在 `defineStore` 第三个参数中配置 `persist: true`。

## Store 拆分原则

- 按业务领域拆分，每个模块单独一个文件
- 禁止创建巨型 store，单个 store 文件不超过 **200 行**
- Store 之间可以相互引用，但禁止循环依赖
